# 출시 4단계 · 계정 보안과 고객지원

2026-09-15 구현. 운영 서버/DB 반영과 개인정보 최종 삭제 절차를 완료했다는 의미가 아니다.

## 계정 보안

- JWT를 사용할 때 서명뿐 아니라 현재 회원 존재 여부와 서버의 `auth_version`을 확인한다. 이메일도 토큰의 임의 값 대신 DB에서 읽는다. 기존 버전 없는 JWT는 버전 0에서만 허용한다.
- 현재 비밀번호를 재확인한 뒤 비밀번호 변경 또는 전체 기기 로그아웃을 수행한다. 버전을 올리므로 현재 기기를 포함해 기존 토큰을 재사용할 수 없다. 이미 처리 중이던 다른 요청을 취소하거나 완료한 거래를 되돌린다는 뜻은 아니다.
- 비밀번호 확인 실패를 DB에 기록한다. 5회 실패 후 15분 동안 해당 재확인 작업을 제한한다. 비밀번호는 bcrypt로 보관하며 새 비밀번호의 UTF-8 72바이트 초과를 거절해 잘림을 방지한다. 보안 이벤트에는 비밀번호·토큰을 기록하지 않는다.
- 로그인 자체의 전체 속도 제한, 이메일 소유권 검증, 비밀번호 찾기/재설정, 소셜/본인확인, 닉네임 정책은 추가 작업이다. 이미 로그인한 사람이 현재 비밀번호를 알고 변경하는 기능과 비밀번호 분실 복구는 구분한다.

## 탈퇴 요청

잔액, 미개봉 캡슐, 보관/배송 상품, 진행 중 결제/환불/문의 현황을 읽고 요청을 접수한다. 현재 비밀번호와 명시적 확인 문구를 요구하며, 요청 번호별 중복 접수를 방지한다. 접수 후 취소할 수 있다.

현재 단계는 `REQUEST_ONLY`다. 요청만으로 계정을 비활성화하거나 GP·상품을 소멸하지 않으며, 회원정보를 삭제했다고 표시하지 않는다. 업로드된 정책에 최종 잔액 처리 방향이 확정되지 않았으므로 임의의 소멸 규칙을 추가하지 않았다. 개인정보 삭제·거래 기록의 법정 보존 분리, 재가입 정책과 운영자 최종 처리는 남아 있다. 운영 화면에서도 최종 삭제 버튼을 제공하지 않는다.

## 고객지원과 운영 권한

- 이용자는 본인의 문의 작성·조회·추가 메시지·읽음 표시·종료·다시 열기를 할 수 있다. 구매 주문을 연결할 때 소유권을 확인한다.
- 운영자는 문의 목록과 대화를 확인하고 답변 및 상태 변경을 할 수 있다. `support_staff`에 활성 권한이 있는 계정만 허용한다. HTTP/웹 입력으로 운영 권한을 스스로 얻을 수 없다. 이 작업에서 실제 계정에 권한을 부여하지 않았다.
- 메시지는 문의 내 순서를 부여한다. 같은 요청 번호를 반복해도 한 번만 저장한다. 새 답변 뒤 오래된 화면에서 종료를 누르면 충돌로 거절한다. 읽음 표시는 실제 확인한 순서까지만 반영하므로 뒤늦게 온 답변은 읽지 않음으로 남는다.
- 초기 메시지 저장에 실패하면 문의 자체도 롤백한다. 동일 회원의 미종료 문의 20건·하루 신규 50건·분당 메시지 30건을 제한한다. 운영자의 답변·상태 변경을 이벤트로 기록한다.
- 첨부 사진, 이메일/SMS/푸시 알림, FAQ/공지 편집, 상품·재고·확률 운영, 출고와 최종 탈퇴 처리까지 완료한 관리자 시스템은 아니다.

## API

| 목적 | 경로 |
| --- | --- |
| 계정/운영 기능 확인 | GET `/account/capabilities` |
| 비밀번호 변경 / 전체 기기 로그아웃 | POST `/account/password`, `/account/revoke-sessions` |
| 탈퇴 현황 / 접수 / 요청 복구 / 취소 | GET `/account/closure-check`, POST `/account/closure-requests`, GET `/account/closure-requests/by-request/:key`, POST `/account/closure-requests/:id/cancel` |
| 문의 접수 / 목록 / 대화 | POST / GET `/support/tickets`, GET `/support/tickets/:id?after=0` |
| 메시지 / 상태 / 읽음 | POST `/support/tickets/:id/messages`, `/status`, `/read` |
| 문의 / 메시지 요청 복구 | GET `/support/tickets/by-request/:key`, `/support/messages/by-request/:key` |
| 운영 조회 / 답변 / 상태 | `/staff/support/tickets` 및 하위 경로 |
| 운영 탈퇴 요청 목록 | GET `/staff/account-closure-requests` |

모든 경로는 JWT를 요구한다. 문의 접수, 메시지, 탈퇴 요청은 `Idempotency-Key` UUID를 요구한다. 대화는 메시지 순서 기준 최대 50개씩 조회한다. 입력 필드는 허용 목록으로 검증하며 소유자·권한·상태를 본문으로 덮어쓸 수 없다.

## 웹 동작

마이 → 계정 보안·탈퇴 요청 / 고객지원에서 시작한다. 구매 상세의 문의 버튼으로 주문을 연결한다. 서버 기능이 아직 반영되지 않았으면 그 상태를 명시한다. 서버 연결 실패를 체험 데이터로 바꾸지 않는다.

체험 모드에서는 실제 운영자에게 전달하지 않고 이 기기의 문의/답변을 저장한다. 운영자 역할 전환은 체험 전용이다. 비밀번호 입력 없이 보안 완료 동작만 시뮬레이션하며 실제 비밀번호를 생성·변경·저장하지 않는다.

서버의 문의·탈퇴 복구 기록에는 종류·요청 번호·문의 번호·상태 등 메타데이터만 저장하고 비밀번호·문의 본문은 따로 저장하지 않는다. 응답 유실 시 같은 번호로 조회한다. 서버가 접수하지 않았음을 확인하면 내용을 다시 입력하게 한다. 비밀번호 변경 결과가 불명확하면 자동 재시도하지 않고 새 비밀번호로 로그인해 확인하게 한다.

## 검증과 반영

로컬 빌드·TypeScript·21개 모음 159개 테스트가 통과했다. 신규 SQL 12개·HTTP 6개로 권한/소유권/멱등/읽음/롤백/비밀번호 제한을 확인했다. 실제 다중 연결 PostgreSQL CI에는 중복 접수, 동시 운영자 답변, 보안 버전 변경 경쟁, 중복 탈퇴 요청 4개를 추가했다. 결과는 PR의 최종 CI를 기준으로 한다.

10번째 마이그레이션을 적용한 뒤 동일 버전 서버를 실행해야 한다. `/health/ready`와 PR 검사를 확인한다. 기존 회원 기본 세션 버전은 0이며 GP와 상품 값을 바꾸지 않는다. 보안/문의/탈퇴 기록이 생긴 뒤 마이그레이션을 되돌리는 자동 삭제는 거절한다.
