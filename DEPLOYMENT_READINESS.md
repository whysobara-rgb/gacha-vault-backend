# 서버 반영 준비 · 2026-09-15

누적 변경은 [PR #3](https://github.com/whysobara-rgb/gacha-vault-backend/pull/3)에 제출했다. 최초 제출 커밋 `fe6de1a89214335c5bf312db6e356334b03c0ed9`의 GitHub Actions에서 빌드·135개 검사와 PostgreSQL 16의 36개 검사가 통과했다. [실행 기록](https://github.com/whysobara-rgb/gacha-vault-backend/actions/runs/34924759590).

이후 이 PR에 DB 준비 상태 점검과 컴파일된 마이그레이션 실행 경로를 추가했다. 최종 커밋의 CI도 통과해야 반영할 수 있다. 이 작업에서 Render 서비스 설정이나 운영 DB는 변경하지 않았다.

## 실행 중과 준비 완료 구분

| 경로 | 의미 | 실패 시 |
| --- | --- | --- |
| GET `/health` | API 프로세스 응답 | 서비스 응답 실패 |
| GET `/health/ready` | DB 연결 및 실행 코드와 정확히 일치하는 적용 마이그레이션 목록 | HTTP 503 |

readiness는 읽기만 수행한다. 마이그레이션 누락, 연결 실패, 마이그레이션 코드 누락, DB보다 오래된 코드로의 롤백 모두 준비 실패로 처리한다. 응답은 캐시하지 않으며 DB 접속 정보나 원문 오류를 노출하지 않는다. readiness 성공이 결제 가맹점·운영 정책·전체 상품 데이터까지 검증했다는 뜻은 아니다.

## 반영 순서

1. PR 최신 커밋의 verify / postgres 검사를 확인한다. 해당 커밋을 별도 테스트 서버와 DB에 먼저 적용한다.
2. 대상 DB와 기존 백업·복원 가능 여부를 확인한다. 기존 GP/상품/주문 기록을 보존한다. 새 DB를 기존 DB 대신 연결하거나 테스트 시드를 운영 DB에 실행하지 않는다.
3. 빌드 환경에서 `npm ci --include=dev` 및 `npm run build`를 실행한다. 설치 버전은 잠금 파일을 따른다.
4. 실행할 서버와 동일한 DB 환경에서 `npm run migration:show:compiled`로 대상 변경을 확인한다. `npm run migration:run:compiled`는 지정한 DB를 변경하므로 배포 단계에서 단일 실행자로 수행한다. 이 명령은 전체 마이그레이션을 한 트랜잭션으로 적용하며 TypeScript 실행기 없이 빌드된 코드로 동작한다.
5. 마이그레이션 성공 후 동일 커밋의 `npm run start:prod`로 실행한다. Render의 상태 점검 경로에 `/health/ready`를 설정한다. 시작 명령 자체는 자동 마이그레이션을 수행하지 않는다.
6. `/health/ready` 성공 후 로그인·카탈로그·확률·계정별 거래 조회 및 테스트 계정의 구매/개봉/환불을 확인한다. 서버가 제공하지 않는 기능은 웹에서 준비 상태로 남는다.

DB가 업데이트된 뒤 이전 코드로 자동 롤백하면 readiness가 실패할 수 있다. 거래 기록이 생성된 뒤 마이그레이션을 되돌려 지우는 방식으로 복구하지 않는다. 필요한 경우 새 버전에서 호환 수정한다.

## 운영 출시 전 남은 항목

- 실제 다날 테스트 인증창·승인·취소, 미확정 거래 조회·정산 대사·영수증
- 상품 실물/현금 구매/환불 대상 분류, 운영 휴일표·배송 요금·복구 정책
- 회원 복구·본인확인·탈퇴, 관리자 권한·문의 처리·출고·배송 추적
- Flutter 반영·Android/iPhone 실기기 검수·스토어 제출 자료

운영 결제는 현재 차단되어 있다. 테스트에서 통과한 것만으로 운영 결제를 켜지 않는다. 가맹점 계약·필수 설정과 운영 검증이 준비된 뒤 코드의 운영 전환을 별도 검토한다.
