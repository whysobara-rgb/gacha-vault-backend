# 배송 거래 구현 · 2026-09-15

## 완료한 범위
- FULFILLMENT_V1: JWT 소유자 한정 견적·신청·상세·목록·취소·요청 번호 조회.
- 최대 100개, 서버 요금표와 10분 견적, 중복 키 재사용, 동일 상품 중복 예약 방지.
- 사용자 → 상품 잠금 순서로 GP 차감·배송 상태·원장·영수증을 한 트랜잭션에서 처리.
- REQUESTED / PREPARING에서만 취소. 원래 배송비 GP 환급, STORED·원래 잠금 설정 복원.
- 전환 잠금은 배송을 막지 않음. 배송비 사용 후 취소·환급돼도 이전 GP 전환 복구 제한은 유지.
- 주소는 견적과 신청 영수증에 보관. 신청 후 주소 변경 API 없음: 취소 후 다시 신청.
- 기존 shipping-requests 읽기 API 유지. 레거시 거래 활성화와 새 배송 기능은 함께 사용하지 않음.

## 활성화와 요금
운영 출시 준비가 끝나기 전에는 `ENABLE_SHIPPING_PREVIEW=false`를 유지합니다.
서버 구현은 `NODE_ENV=development|test`, `ENABLE_SHIPPING_PREVIEW=true`, `ENABLE_LEGACY_TRANSACTIONS!=true`에서만 거래를 허용합니다. production은 항상 비활성입니다.

요금은 `SHIPPING_RATE_TABLE_JSON`으로 명시합니다. 임의 기본 요금은 없습니다. 각 구역은 `{id,label,prefixes,feeGP}`이며 우편번호의 가장 긴 접두어를 적용합니다. `*`는 명시적인 나머지 지역 지원입니다. 중복 접두어·음수 요금·지원하지 않는 우편번호는 거절합니다. 요금 설정 변경 시 이전 견적은 재확인해야 합니다.

웹 체험의 3,000 GP는 가상 예시입니다. 무료 배송 기준·무게·택배사·도서산간 정책이 정해지기 전에는 운영 요금으로 사용하지 않습니다. 상품은 기본값이 `UNSPECIFIED`, 배송 불가이며, 실제 배송 가능 여부를 확인한 상품에만 `PHYSICAL` / `shipping_enabled=true`를 지정합니다. 기존 테스트 상품·운영 상품을 자동으로 변경하는 마이그레이션은 없습니다.

## 상태
`REQUESTED → PREPARING → COLLECTED → SHIPPING → DELIVERED`.
앞의 두 단계에서만 `CANCELLED`로 이동할 수 있습니다. 사용자에게 출고 단계를 임의로 진행시키는 API는 제공하지 않습니다. 운영자 출고 API는 아직 없습니다. 후속 구현도 사용자 → 신청 → 상품의 동일한 잠금 순서를 유지하고 집화/취소 경쟁을 검증해야 합니다.

## 검증 / 제한
- PGlite PostgreSQL WASM: 8개 전체 마이그레이션 및 배송 SQL 수명주기 10개 테스트 통과. 원장 실패 시 거래 롤백, 중복 신청·환급, 100개 묶음, 0 GP 배송, 소유권, 만료·요금 변경·상태 재확인 포함.
- 실제 Nest/JWT HTTP 경계 테스트 6개 통과.
- 백엔드 전체 15개 suite / 111개 test 통과; `npm run build` 성공.
- `test/fulfillments.postgres-spec.ts`에 별도 PostgreSQL 3개 동시성 테스트 작성. 전용 서버를 실행할 수 없어 이번 환경에서는 실행하지 않음. CI에서 `TEST_POSTGRES=true npm run test:postgres` 실행 필요.
- 새 서버 코드는 로컬 커밋 및 웹 프로젝트의 소스 패치로 보존. GitHub 파일 접근 실패로 원본 저장소/Render 반영은 완료되지 않음.

## 출시 전 미완료
- 운영 배송비·가능 지역 확정, 주소 검색·검증 및 도서산간 분류.
- 운영자 권한·출고·재고 대조·운송장·택배사 상태 확인·고객문의 연계.
- 미사용 견적은 다음 견적 요청 때 만료분을 청소하며 사용자당 20개로 제한. 전역 만료 견적 정리 작업, 주소 보존/삭제 기간, 암호화·권한·로그 마스킹 정책은 운영 전에 추가 필요.
- 취소/집화/전환/GP 구매 동시 요청을 실제 다중 연결 DB에서 검증.
- API 배포·웹 서버 연동·Flutter 반영·실제 iOS/Android 검수.
